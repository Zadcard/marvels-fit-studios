import { ClientSettingsWorkspace } from "@/components/dashboard/client-settings-workspace";

export const metadata = {
  title: "Client Settings",
};

export default function ClientSettingsPage() {
  return <ClientSettingsWorkspace />;
}
