import { ClientCoachWorkspace } from "@/components/dashboard/client-coach-workspace";

export const metadata = {
  title: "My Coach",
};

export default function ClientCoachPage() {
  return <ClientCoachWorkspace />;
}
