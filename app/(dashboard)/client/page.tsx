import { ClientOverviewWorkspace } from "@/components/dashboard/client-overview-workspace";

export const metadata = {
  title: "Client Dashboard",
};

export default function ClientOverviewPage() {
  return <ClientOverviewWorkspace />;
}
