import { CoachOverviewWorkspace } from "@/components/dashboard/coach-overview-workspace";

export const metadata = {
  title: "Coach Overview",
};

export default function CoachOverviewPage() {
  return <CoachOverviewWorkspace />;
}
